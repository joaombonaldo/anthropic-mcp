# Copy to .env and fill in. This course runs a local Ollama model via a
# LiteLLM proxy, so the key is a dummy and the base URL is local.
CLAUDE_MODEL="ollama_chat/qwen2.5"
ANTHROPIC_API_KEY="sk-dummy-local"
ANTHROPIC_BASE_URL="http://localhost:4000"
USE_UV=1
